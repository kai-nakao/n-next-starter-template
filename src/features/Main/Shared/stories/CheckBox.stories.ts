import { Meta } from '@storybook/react'

import { CheckBox } from '../components/CheckBox'

const meta: Meta<typeof CheckBox> = {
  component: CheckBox,
  title: 'Main/Shared/CheckBox',
}

export default meta
export const BaseCheckBox = {}
